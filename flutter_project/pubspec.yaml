name: badal_app
description: "تطبيق بدل للخدمات والعملات - تطبيق فلاتر احترافي لتبادل العملات والمنتجات الغذائية"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5
  provider: ^6.0.5
  google_fonts: ^6.1.0
  intl: ^0.18.1
  url_launcher: ^6.1.11
  flutter_screenutil: ^5.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/images/
